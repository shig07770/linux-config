export GAMES_DIR="$HOME/Games"
mkdir -p "$GAMES_DIR"

export MUSIC_DIR="$HOME/Music"
mkdir -p "$MUSIC_DIR"

export VIDEOS_DIR="$HOME/Videos"
mkdir -p "$VIDEOS_DIR"

export PUBLIC_DIR="$HOME/Public"
mkdir -p "$PUBLIC_DIR"

export DEV_DIR="$HOME/Development"
mkdir -p "$DEV_DIR"

export DESKTOP_DIR="$HOME/Desktop"
mkdir -p "$DESKTOP_DIR"

export PICTURES_DIR="$HOME/Pictures"
mkdir -p "$PICTURES_DIR"

export TEMPLATES_DIR="$HOME/Templates"
mkdir -p "$TEMPLATES_DIR"

export DOCUMENTS_DIR="$HOME/Documents"
mkdir -p "$DOCUMENTS_DIR"

export DOWNLOADS_DIR="$HOME/Downloads"
mkdir -p "$DOWNLOADS_DIR"

export PROJECTS_DIR="$DEV_DIR/Projects"
mkdir -p "$PROJECTS_DIR"

export REPO_RAW_URL="https://rawgithubusercontent.com/shig07770/config/master"
